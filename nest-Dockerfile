FROM node:latest 

WORKDIR /app

COPY ./src/back/package*.json .

RUN npm install

RUN npm run build

EXPOSE 3001

# ENTRYPOINT [ "npx", "prisma", "migrate", "dev" ]

CMD ["npm", "run", "start:dev"]

